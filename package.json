{
  "name": "com.flux.capture",
  "version": "1.0.0",
  "description": "Flux Capture - Intelligent Document Capture for NetSuite",
  "private": true,
  "scripts": {
    "sync": "node scripts/sync.js",
    "sync:all": "node scripts/sync.js --all",
    "sync:deploy": "node scripts/sync.js --deploy",
    "sync:watch": "node scripts/sync.js --watch",
    "build": "node scripts/build-distribution.js",
    "build:bundle": "node scripts/build-distribution.js --bundle",
    "build:obfuscate": "node scripts/build-distribution.js --obfuscate",
    "verify-installation": "node scripts/verify-installation.js"
  },
  "devDependencies": {
    "javascript-obfuscator": "^4.1.0",
    "archiver": "^6.0.1",
    "glob": "^10.3.10"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
